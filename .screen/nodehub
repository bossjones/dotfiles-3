source "$HOME/.screenrc"

chdir "$HOME/src/nodehub/nodehub"
screen -t code0  0

chdir "$HOME/src/nodehub/nodehub"
screen -t code1  1

chdir "$HOME/src/nodehub/gui"
screen -t gui0  2

chdir "$HOME/src/nodehub/gui"
screen -t gui1  3

chdir "$HOME/src/nodehub"
screen -t django 4

chdir "$HOME/src/nodehub"
screen -t plovr 5

chdir "$HOME/src/nodehub"
screen -t shell 6

chdir "$HOME/src/nodehub"
