source "$HOME/.screenrc"

chdir "$HOME/rpmbuild"
screen -t rpmbuild  0

chdir "$HOME/rpmbuild/SOURCES"
screen -t sources  1

chdir "/var/lib/mock"
screen -t mock  2

chdir "$HOME/src/rpms"
screen -t rpms  3

chdir "$HOME"
