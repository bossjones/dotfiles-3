source "$HOME/.screenrc"

chdir "$HOME/src/raku/web/lib"
screen -t web-code 0

chdir "$HOME/src/raku/web/test"
screen -t web-test 1

chdir "$HOME/src/raku/web/views"
screen -t web-views 2

chdir "$HOME/src/raku/web/stylus"
screen -t web-stylus 3

chdir "$HOME/src/raku/web/static"
screen -t web-js 4

chdir "$HOME/src/raku"
screen -t run 5

chdir "$HOME/src/raku"
screen -t shell 6

chdir "$HOME/src/raku"
