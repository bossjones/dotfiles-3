export FACTER_EMAIL="${FACTER_EMAIL:-silas@sewell.org}"
export FACTER_HOME="$HOME"

sync() {
  git submodule init && \
  git submodule update && \
  git submodule foreach git submodule init && \
  git submodule foreach git submodule update
}
