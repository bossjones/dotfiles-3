source "$HOME/.screenrc"

chdir "$HOME/src/dockstack"
screen -t code 0

chdir "$HOME/src/dockstack/static"
screen -t js 1
screen -t templates 2

chdir "$HOME/src/dockstack"
screen -t run 3
screen -t shell 4
